import 'package:flutter/material.dart';

class QuesMP extends StatefulWidget {
  const QuesMP({Key? key}) : super(key: key);

  @override
  State<QuesMP> createState() => _QuesMPState();
}

class _QuesMPState extends State<QuesMP> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
