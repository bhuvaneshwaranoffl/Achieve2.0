import 'package:flutter/material.dart';

class NotesSensor extends StatefulWidget {
  const NotesSensor({Key? key}) : super(key: key);

  @override
  State<NotesSensor> createState() => _NotesSensorState();
}

class _NotesSensorState extends State<NotesSensor> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
