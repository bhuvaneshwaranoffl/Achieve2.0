import 'package:flutter/material.dart';

class YouSensor extends StatefulWidget {
  const YouSensor({Key? key}) : super(key: key);

  @override
  State<YouSensor> createState() => _YouSensorState();
}

class _YouSensorState extends State<YouSensor> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
