import 'package:flutter/material.dart';

class QuesSensor extends StatefulWidget {
  const QuesSensor({Key? key}) : super(key: key);

  @override
  State<QuesSensor> createState() => _QuesSensorState();
}

class _QuesSensorState extends State<QuesSensor> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
