import 'package:flutter/material.dart';

class NotesPython extends StatefulWidget {
  const NotesPython({Key? key}) : super(key: key);

  @override
  State<NotesPython> createState() => _NotesPythonState();
}

class _NotesPythonState extends State<NotesPython> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
