import 'package:flutter/material.dart';

class YouChe extends StatefulWidget {
  const YouChe({Key? key}) : super(key: key);

  @override
  State<YouChe> createState() => _YouCheState();
}

class _YouCheState extends State<YouChe> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
