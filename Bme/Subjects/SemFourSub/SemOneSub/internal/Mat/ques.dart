import 'package:flutter/material.dart';

class QuesMatrix extends StatefulWidget {
  const QuesMatrix({Key? key}) : super(key: key);

  @override
  State<QuesMatrix> createState() => _QuesMatrixState();
}

class _QuesMatrixState extends State<QuesMatrix> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
