import 'package:flutter/material.dart';

class NotesMatrix extends StatefulWidget {
  const NotesMatrix({Key? key}) : super(key: key);

  @override
  State<NotesMatrix> createState() => _NotesMatrixState();
}

class _NotesMatrixState extends State<NotesMatrix> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
