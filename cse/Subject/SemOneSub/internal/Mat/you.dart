import 'package:flutter/material.dart';

class YouMatrix extends StatefulWidget {
  const YouMatrix({Key? key}) : super(key: key);

  @override
  State<YouMatrix> createState() => _YouMatrixState();
}

class _YouMatrixState extends State<YouMatrix> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
